<script type="text/javascript">
$(document).ready(function(){
	
			// Validate form
			$("#edit").validate({
				errorElement: "span", // Định dạng cho thẻ HTML hiện thông báo lỗi
				rules: {
				},
        submitHandler: function(form) {
          var formData = new FormData($("#edit")[0]);
          formData.append('department_id', '<?php echo $department_data->department_id ?>');
          
          $.ajax({
              type: "POST", // phương thức gởi đi
              url: "<?php echo BASE_URL ?>/department/editdepartment", // nơi mà dữ liệu sẽ chuyển đến khi submit
              data: formData, // giá trị post
              cache: false,
              contentType: false,
              processData: false,
              success: function(answer){ // if everything goes well
                  //alert(answer);
                  $('#error').hide();
                  $('#error').slideToggle(100); // hiển thị thẻ div success
                  $('#error').html('<div class="successbox">'+answer+'</div>'); // đặt kết quả trả về từ test.php vào thẻ div success
                  $('#error').fadeOut(10000);

              }
          });
          return false;
           
       }
					 
				 
			});
	
});
</script>

        <div class="login_body">
         
              <form id="edit" method="post" action="">
                  <table style="margin-top:20px;">
                    
                    <tr>
                        <td>Mã phòng ban</td>
                        <td><input type="text" id="department_code" name="department_code" tabindex="1" required="required" autofocus value="<?php echo $department_data->department_code ?>"></td>
                        
                    </tr>
                    <tr>
                        <td>Tên phòng ban</td>
                        <td><input type="text" id="department_name" name="department_name" tabindex="2" required="required" value="<?php echo $department_data->department_name ?>"></td>
                    </tr>
                </table> 
            </form>
        </div>
        <div id="error" class="error"><?php echo isset($error) ? $error : null; ?></div>


